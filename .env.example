# Backend API base URL（后端服务地址），例：https://tej69zzg.sg-fn.tiktok-row.net
# 本地开发：复制此文件为 .env 并填写；线上：在 Vercel 项目的 Environment Variables 中配置。
# 未配置时前端以离线模式运行：提交按钮生成本地 Payload，可通过 Agent Payload 面板复制使用。
#
# Auto Report 后端接口（见 src/api.ts）：
#   POST   {BASE}/api/auto_report/data_templates                              新增数据模板（一个 Template ID 一条）
#   GET    {BASE}/api/auto_report/data_templates/{chart_template_id}          查询单个数据模板
#   DELETE {BASE}/api/auto_report/data_templates?data_template_id={id}        删除数据模板
#   GET    {BASE}/api/auto_report/data_templates?keyword=&page=&page_size=    查询数据模板列表
#   POST   {BASE}/api/auto_report/report_data_templates                       新增报告数据模板关系
#   PUT    {BASE}/api/auto_report/report_data_templates/{report_config_id}    更新报告数据模板关系
#   GET    {BASE}/api/auto_report/report_data_templates/{report_config_id}    查询单个报告数据模板关系
#   DELETE {BASE}/api/auto_report/report_data_templates?report_config_id={id} 删除报告数据模板关系
#   GET    {BASE}/api/auto_report/report_data_templates?keyword=&page=&page_size=&expand_templates=  查询列表
#
#   GET  {BASE}/api/superchart/templates                          （预留）SuperChart 模板列表
#   POST {BASE}/api/superchart/templates/{id}/batch-update        （预留）SuperChart 批量数据更新
#   POST {BASE}/api/qwen/chat                                     （预留）千问AI 对话  { prompt, context } -> { text }
VITE_API_BASE_URL=
